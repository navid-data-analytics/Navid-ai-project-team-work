package postgres

// Log messages
const (
	LogMsgNewSession = "Created new postgres connection"
)

// Log message field keys
const (
	LogKeyCurrentSecretID = "currentSecretID"
	LogKeyPrevSecretID    = "prevSecretID"
)
